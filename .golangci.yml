linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports

linters-settings:
  goimports:
    local-prefixes: tools

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0

